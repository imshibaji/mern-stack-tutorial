extends ../layout

block content
    h1.text-center Contact List
    .row.justify-content-center
        table.table
            tr
                th Name
                th Email
                th Mobile
                th Message
            each contact in contacts
                tr
                    td=contact.name
                    td=contact.email
                    td=contact.mobile
                    td=contact.message